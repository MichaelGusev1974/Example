// Определение большего из двух чисел с помощью метода класса System.Math.
int firstValue = 500;
int secondValue = 600;
int largerValue;
largerValue = Math.Max(firstValue, secondValue );
Console.WriteLine(largerValue);
